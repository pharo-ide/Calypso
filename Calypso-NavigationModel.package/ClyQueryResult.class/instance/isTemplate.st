testing
isTemplate
	"I am used as template in queries to describe required result.
	In that case I have no assigned environment. 
	During query execution template result prepares actual result instance
	which is bound to given query environment"
	^environment isNil