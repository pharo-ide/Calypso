testing
isBoundToEnvironment
	"Unknown scope do not need any environment to process queries. But queries required it. 
	So we think that unknown scope is always bound to some environment while it not needs to be set"
	^true